<!-- Temporary dev-phase file for windro's fork. Safe to delete. -->

# Read AGENTS.md first

This is **windro's fork** of NousResearch/hermes-agent, not the original.

The fork rules are the `FORK-RULES` block at the top of [AGENTS.md](./AGENTS.md).
Read it before any git operation, any edit, and before running any test. It covers
which remote you may push to, why upstream is never synced automatically, the
branch-test-approve workflow, and one test file that must never be run on this
machine.
